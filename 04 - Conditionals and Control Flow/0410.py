# Use boolean expressions as appropriate on the lines below!

# Make me false!
bool_one = (2 <= 2) and "Alpha" == "Bravo"  # We did this one for you!

# Make me true!
bool_two = 25 ** 0.5 > 2 ** 2 or "a" != "b"

# Make me false!
bool_three = not not "asdf" == "hjkl"

# Make me true! 
bool_four = (1 - 2) ** 2 > 0 and "a" + "b" == "ab"

# Make me true!
bool_five = not False